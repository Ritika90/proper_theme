
indexer
{
}

searchd
{
  listen = 127.0.0.1:9313:mysql41
  log = /home/ritika/my_projects/proper/log/development.searchd.log
  query_log = /home/ritika/my_projects/proper/log/development.searchd.query.log
  pid_file = /home/ritika/my_projects/proper/log/development.sphinx.pid
  workers = threads
  binlog_path = /home/ritika/my_projects/proper/tmp/binlog/development
}

source property_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = root
  sql_db = sphinx
  sql_query_pre = SET TIME_ZONE = '+0:00'
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT SQL_NO_CACHE `properties`.`id` * 2 + 0 AS `id`, properties.`name` AS `name`, properties.`id` AS `sphinx_internal_id`, 'Property' AS `sphinx_internal_class`, 0 AS `sphinx_deleted` FROM `properties`  WHERE (`properties`.`id` BETWEEN $start AND $end) GROUP BY `properties`.`id`, properties.`name`, properties.`id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`properties`.`id`), 1), IFNULL(MAX(`properties`.`id`), 1) FROM `properties`
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = sphinx_deleted
  sql_attr_string = sphinx_internal_class
  sql_query_info = SELECT `properties`.* FROM `properties`  WHERE (`properties`.`id` = ($id - 0) / 2)
}

index property_core
{
  type = plain
  path = /home/ritika/my_projects/proper/db/sphinx/development/property_core
  docinfo = extern
  charset_type = utf-8
  source = property_core_0
}

index property
{
  type = distributed
  local = property_core
}
